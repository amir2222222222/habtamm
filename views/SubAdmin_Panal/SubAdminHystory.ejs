<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction History</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #4e73df;
            --secondary-color: #f8f9fc;
            --accent-color: #2e59d9;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            background: #f5f7fa;
            color: var(--dark);
            padding-top: 5rem;
            min-height: 100vh;
            
        }

        .card {
            border: none;
            border-radius: 0.35rem;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.1);
            margin-bottom: 2rem;
        }

        .card-header {
            background-color: white;
            border-bottom: 1px solid #e3e6f0;
            padding: 1rem 1.35rem;
            border-radius: 0.35rem 0.35rem 0 0 !important;
        }

        .card-header h4 {
            font-weight: 700;
            color: #4e73df;
            margin-bottom: 0;
        }

        .total-badge {
            background-color: var(--primary-color);
            color: white;
            font-size: 1rem;
            padding: 0.5rem 1rem;
            border-radius: 0.35rem;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
        }

        .transaction-table {
            margin-top: 20px;
        }

        .table th {
            background-color: #f8f9fc;
            color: #5a5c69;
            font-weight: 700;
            border-top: 1px solid #e3e6f0;
        }

        .table td {
            vertical-align: middle;
        }

        .total-row {
            font-weight: bold;
            background-color: #f8f9fa;
        }

        .text-danger {
            color: #e74a3b !important;
        }

        .text-success {
            color: #1cc88a !important;
        }

        .user-info {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1.5rem;
            padding: 1rem;
            background-color: white;
            border-radius: 0.35rem;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.1);
        }

        .user-info h2 {
            margin-bottom: 0;
            color: #5a5c69;
            font-weight: 700;
        }

        .badge-icon {
            margin-right: 8px;
        }

        .table-responsive {
            border-radius: 0.35rem;
            overflow: hidden;
        }

        .table-striped tbody tr:nth-of-type(odd) {
            background-color: rgba(0, 0, 0, 0.02);
        }
    </style>
</head>
<%- include('../partials/SubAdminHeader') -%>

    <body>
        <div class="container">
            <div class="user-info">
                <h2><i class="fas fa-user-circle me-2"></i>Transaction History for <%= subadminName %>
                </h2>
                <div class="total-badge">
                    <i class="fas fa-wallet badge-icon"></i>Total Credited: <%= totalCredited.toFixed(2) %>
                </div>
            </div>

            <div class="card transaction-table">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h4><i class="fas fa-list me-2"></i>All Transactions</h4>
                    <div>
                        <span class="badge bg-primary">
                            Total Records: <%= transactions.length %>
                        </span>
                    </div>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Date</th>
                                    <th>Credited To</th>
                                    <th>Amount</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% transactions.forEach((transaction, index)=> { %>
                                    <tr>
                                        <td>
                                            <%= index + 1 %>
                                        </td>
                                        <td>
                                            <%= new Date(transaction.date).toLocaleString() %>
                                        </td>
                                        <td>
                                            <%= transaction.deposited_user_userName %>
                                        </td>
                                        <td class="text-danger">-<%= Math.abs(transaction.amount).toFixed(2) %>
                                        </td>
                                    </tr>
                                    <% }); %>
                                        <tr class="total-row">
                                            <td colspan="3">Total Credited Amount</td>
                                            <td class="text-success">
                                                <%= totalCredited.toFixed(2) %>
                                            </td>
                                        </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
    </body>

</html>